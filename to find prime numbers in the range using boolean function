#include<stdbool.h> 
#include <stdio.h>
bool isPrime(int n){
   int count=0;
    for(int i=1;i<=n;i++){
        if(n%i==0)
        count++;
    }
    if(count==2)
    return true;
    else
    return false;
    return 0;
}

int main()
{
    int n;
    scanf("%d",&n);
    for(int i=1;i<=n;i++){
        if(isPrime(i))
        printf("%d ",i);
    }
    
   

    return 0;
}
