#include <stdio.h>

int isPrime(int n) {
    if (n <= 1)
        return 0; 
    for (int i = 2; i * i <= n; i++) {
        if (n % i == 0)
            return 0; 
    }
    return 1;
}

int main() {
    int s, count = 0;
    scanf("%d", &s);
    for (int i = 2; i <= s; i++) {
        if (isPrime(i)) {
            count++;  
            if (count % 2 != 0)  
                printf("%d ", i);
        }
    }

    return 0;
}
